name: "experiment 1"
base_dir: "./outputs"
settings: # the base cofiguration of the experiment

trials: # each trial changes the parameters it chooses
  - name: trial_1
    settings:
      dataset:
        len: 1000

  - name: trial_2
    settings:
      dataset:
        len: 1250

  - name: trial_3
    settings:
      dataset:
        len: 1500